#track income, expense, and generate reports
income_input = {}
expense_input = {}
income_input = input('my income: ')
expense_input = input('my expenses: ')
#csv files for storage
for items in income_input,expense_input:
  for price in income_input,expense_input:
    CSV_files = {}
CSV_files= print(CSV_files.update(income_input))
CSV_files = print(CSV_files.update(expense_input))
    



#add functions,totals, generate monthly summaries

